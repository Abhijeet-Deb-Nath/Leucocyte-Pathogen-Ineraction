# Paper Figures

This folder stores figure assets that belong to the manuscript package itself rather than to generated result outputs.

Keep here:

- conceptual diagrams
- control-loop schematics
- static benchmark illustrations used in the paper text

Do not duplicate generated result plots here. Those remain in `results/paper_ready/`.
